import React from 'react';
import { SessionData } from '../types';

interface GeminiTutorProps {
  session: SessionData;
}

// Component này đã bị vô hiệu hóa để loại bỏ chức năng AI.
const GeminiTutor: React.FC<GeminiTutorProps> = ({ session }) => {
  return null;
};

export default GeminiTutor;